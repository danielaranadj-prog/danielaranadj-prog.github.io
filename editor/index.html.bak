<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Publicaciones - Blog.Instantetrips</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <script
        src="https://cdn.jsdelivr.net/npm/browser-image-compression@2.0.2/dist/browser-image-compression.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>

    <!-- Tiptap will be loaded via npm modules -->
    <script src="https://unpkg.com/turndown/dist/turndown.js"></script>
    <script src="https://unpkg.com/turndown-plugin-gfm/dist/turndown-plugin-gfm.js"></script>

    <script type="module" src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.esm.js"></script>
    <script nomodule src="https://unpkg.com/ionicons@7.1.0/dist/ionicons/ionicons.js"></script>

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: { sans: ['Inter', 'sans-serif'] },
                    colors: { slate: { 850: '#1e293b' } }
                }
            }
        }
    </script>

    <style>
        body {
            background-color: #f3f4f6;
            color: #111;
            font-family: 'Inter', sans-serif;
            transition: background 0.3s, color 0.3s;
        }

        body.dark {
            background-color: #0f172a;
            color: #e2e8f0;
        }

        .logo-adaptive {
            filter: invert(0);
        }

        .dark .logo-adaptive {
            filter: invert(0);
        }

        /* Tiptap Editor Styles */
        .tiptap-editor {
            border-color: #334155 !important;
        }

        /* Tiptap Toolbar Buttons */
        .toolbar-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 2px;
            min-width: 32px;
            height: 32px;
            padding: 4px 8px;
            background: white;
            border: 1px solid #e5e7eb;
            border-radius: 6px;
            font-size: 14px;
            font-weight: 600;
            color: #374151;
            cursor: pointer;
            transition: all 0.2s;
        }

        .dark .toolbar-btn {
            background: #1e293b;
            border-color: #475569;
            color: #e2e8f0;
        }

        .toolbar-btn:hover {
            background: #f3f4f6;
            border-color: #f97316;
            color: #f97316;
            transform: translateY(-1px);
        }

        .dark .toolbar-btn:hover {
            background: #334155;
            border-color: #f97316;
        }

        .toolbar-btn:active {
            transform: translateY(0);
        }

        /* Glassmorphism */
        .glass-card {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.15), 0 0 0 1px rgba(255, 255, 255, 0.1);
        }

        .dark .glass-card {
            background: rgba(15, 23, 42, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* V7 Sidebar Styles */
        #v7-sidebar {
            position: fixed;
            left: 0;
            top: 0;
            bottom: 0;
            width: 280px;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-right: 1px solid rgba(0, 0, 0, 0.1);
            z-index: 40;
            transition: transform 0.3s ease, width 0.3s ease;
            display: flex;
            flex-direction: column;
        }

        .dark #v7-sidebar {
            background: rgba(15, 23, 42, 0.95);
            border-right: 1px solid rgba(255, 255, 255, 0.1);
        }

        #v7-sidebar.collapsed {
            width: 80px;
        }

        /* Sidebar Header */
        .sidebar-header {
            padding: 1.5rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }

        .dark .sidebar-header {
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        /* Sidebar Tabs */
        .sidebar-tabs {
            display: flex;
            flex-direction: column;
            gap: 0.5rem;
            padding: 1rem;
        }

        .sidebar-tab-btn {
            display: flex;
            align-items: center;
            gap: 0.75rem;
            padding: 0.75rem 1rem;
            background: transparent;
            border: none;
            border-radius: 0.75rem;
            cursor: pointer;
            transition: all 0.2s;
            font-size: 0.875rem;
            font-weight: 500;
            color: #64748b;
            text-align: left;
        }

        .dark .sidebar-tab-btn {
            color: #94a3b8;
        }

        .sidebar-tab-btn:hover {
            background: rgba(249, 115, 22, 0.1);
            color: #f97316;
        }

        .sidebar-tab-btn.active {
            background: linear-gradient(135deg, #f97316 0%, #fb923c 100%);
            color: white;
            box-shadow: 0 4px 12px rgba(249, 115, 22, 0.3);
        }

        .sidebar-tab-btn ion-icon {
            font-size: 1.25rem;
            flex-shrink: 0;
        }

        #v7-sidebar.collapsed .sidebar-tab-btn span {
            display: none;
        }

        #v7-sidebar.collapsed .sidebar-tab-btn {
            justify-content: center;
            padding: 0.75rem;
        }

        /* Main Content with Sidebar */
        #v7-main-content {
            margin-left: 280px;
            transition: margin-left 0.3s ease;
            min-height: 100vh;
        }

        #v7-main-content.sidebar-collapsed {
            margin-left: 80px;
        }

        /* Tab Content */
        .tab-content {
            display: none;
        }

        .tab-content:not(.hidden) {
            display: block;
        }

        /* Collapse Button */
        .sidebar-collapse-btn {
            position: absolute;
            right: -12px;
            top: 50%;
            transform: translateY(-50%);
            width: 24px;
            height: 24px;
            background: white;
            border: 1px solid rgba(0, 0, 0, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.2s;
            z-index: 41;
        }

        .dark .sidebar-collapse-btn {
            background: #1e293b;
            border-color: rgba(255, 255, 255, 0.1);
        }

        .sidebar-collapse-btn:hover {
            background: #f97316;
            border-color: #f97316;
            color: white;
        }

        .sidebar-collapse-btn ion-icon {
            font-size: 0.875rem;
            transition: transform 0.3s ease;
        }

        #v7-sidebar.collapsed .sidebar-collapse-btn ion-icon {
            transform: rotate(180deg);
        }

        /* Responsive */
        @media (max-width: 1024px) {
            #v7-sidebar {
                transform: translateX(-100%);
            }

            #v7-sidebar.mobile-open {
                transform: translateX(0);
            }

            #v7-main-content {
                margin-left: 0;
            }
        }

        /* Gradient Background */
        .gradient-bg {
            background: linear-gradient(-45deg, #f3f4f6, #dbeafe, #fce7f3, #ffedd5, #f3f4f6);
            background-size: 400% 400%;
            animation: gradientShift 20s ease infinite;
        }

        .dark .gradient-bg {
            background: linear-gradient(-45deg, #0f172a, #1e1b4b, #1e293b, #0f172a);
            background-size: 400% 400%;
        }

        @keyframes gradientShift {

            0%,
            100% {
                background-position: 0% 50%;
            }

            25% {
                background-position: 50% 100%;
            }

            50% {
                background-position: 100% 50%;
            }

            75% {
                background-position: 50% 0%;
            }
        }

        /* Card Hover Effect */
        .card-hover {
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .card-hover:hover {
            transform: translateY(-4px);
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
        }

        /* Accent Input */
        .accent-input {
            transition: all 0.2s;
        }

        .accent-input:focus {
            outline: none;
            border-color: #f97316;
            box-shadow: 0 0 0 3px rgba(249, 115, 22, 0.1);
        }

        /* Skeleton Loading */
        .skeleton {
            background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
            background-size: 200% 100%;
            animation: loading 1.5s infinite;
        }

        .dark .skeleton {
            background: linear-gradient(90deg, #1e293b 25%, #334155 50%, #1e293b 75%);
            background-size: 200% 100%;
        }

        @keyframes loading {
            0% {
                background-position: 200% 0;
            }

            100% {
                background-position: -200% 0;
            }
        }

        /* Modal */
        .modal {
            display: none;
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(4px);
            z-index: 50;
            align-items: center;
            justify-content: center;
        }

        .modal.open {
            display: flex;
        }

        /* Toast */
        .toast {
            animation: slideIn 0.3s ease-out;
        }

        @keyframes slideIn {
            from {
                transform: translateX(-100%);
                opacity: 0;
            }

            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        /* Hide by default */
        .hidden {
            display: none !important;
        }

        /* CTA Destination Card Styles */
        .cta-destination-card {
            display: block;
            margin: 2rem 0;
            padding: 1.5rem;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 1rem;
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .cta-destination-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.4);
        }

        .cta-destination-card a {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            text-decoration: none;
            color: white;
        }

        .cta-emoji {
            font-size: 3rem;
            flex-shrink: 0;
        }

        .cta-content {
            flex: 1;
        }

        .cta-label {
            display: block;
            font-size: 0.75rem;
            font-weight: 700;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            opacity: 0.9;
            margin-bottom: 0.25rem;
        }

        .cta-title {
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0 0 0.5rem 0;
            color: white;
        }

        .cta-description {
            font-size: 0.95rem;
            opacity: 0.95;
            margin: 0;
            line-height: 1.5;
        }
    </style>
</head>

<body class="gradient-bg">

    <!-- Login Section -->
    <div id="login-section" class="min-h-screen flex items-center justify-center p-6">
        <div class="glass-card p-10 rounded-3xl shadow-2xl w-full max-w-md">
            <div class="text-center mb-8">
                <img src="public/blog.svg" alt="Logo" class="h-14 mx-auto mb-4 logo-adaptive">
                <h1
                    class="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-600 dark:from-white dark:to-gray-300 bg-clip-text text-transparent">
                    Editor V6.4</h1>
                <p class="text-xs text-gray-400 mt-1">Panel de Publicaciones</p>
            </div>
            <form id="login-form" class="space-y-4">
                <input type="email" id="login-email" placeholder="Email" required
                    class="w-full p-4 rounded-xl accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600">
                <input type="password" id="login-password" placeholder="Contraseña" required
                    class="w-full p-4 rounded-xl accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600">
                <button type="submit"
                    class="w-full py-4 bg-gradient-to-r from-orange-500 to-pink-500 text-white font-bold rounded-xl shadow-lg hover:shadow-xl transition-all hover:scale-105">
                    Entrar
                </button>
            </form>
        </div>
    </div>

    <!-- Main App Section (Hidden by default) -->
    <div id="app-section" class="hidden">

        <!-- V7 Mockup Layout: 3 Columns -->
        <div id="v7-layout" class="flex h-screen overflow-hidden">

            <!-- 1. Icon Sidebar (Left) -->
            <aside id="icon-sidebar" class="w-[70px] bg-[#1a1a1a] flex flex-col items-center py-6 gap-6">
                <button class="icon-btn active" data-section="editor" title="Editor">
                    <ion-icon name="home-outline"></ion-icon>
                </button>
                <button class="icon-btn" data-section="posts" title="Publicaciones">
                    <ion-icon name="document-text-outline"></ion-icon>
                </button>
                <button class="icon-btn" data-section="media" title="Medios">
                    <ion-icon name="images-outline"></ion-icon>
                </button>
                <button class="icon-btn" data-section="video" title="Videos">
                    <ion-icon name="videocam-outline"></ion-icon>
                </button>

                <div class="flex-1"></div>

                <button class="icon-btn" data-section="settings" title="Configuración">
                    <ion-icon name="settings-outline"></ion-icon>
                </button>
                <button class="icon-btn" data-section="user" title="Usuario">
                    <ion-icon name="person-outline"></ion-icon>
                </button>
                <button onclick="toggleDarkMode()" class="icon-btn" title="Tema">
                    <ion-icon name="moon-outline" class="dark:hidden"></ion-icon>
                    <ion-icon name="sunny-outline" class="hidden dark:block"></ion-icon>
                </button>
                <button onclick="logout()" class="icon-btn text-red-400" title="Salir">
                    <ion-icon name="log-out-outline"></ion-icon>
                </button>
            </aside>

            <!-- 2. Config Panel (Left-Middle) -->
            <aside id="config-panel"
                class="w-[320px] bg-white dark:bg-slate-900 border-r border-gray-200 dark:border-slate-700 overflow-y-auto">
                <div class="p-6 space-y-6">

                    <!-- Header -->
                    <div class="flex items-center justify-between">
                        <h2 class="text-lg font-bold">Configuración del Artículo</h2>
                        <button onclick="publishCurrentPost()"
                            class="px-3 py-1 bg-orange-500 text-white text-sm rounded-lg hover:bg-orange-600 transition-colors">
                            Publicar
                        </button>
                    </div>

                    <!-- Title -->
                    <div>
                        <label class="block text-sm font-semibold mb-2">Título</label>
                        <input type="text" id="post-title" placeholder="Título del post..." oninput="updateSEO()"
                            class="w-full p-3 rounded-lg border border-gray-200 dark:bg-slate-800 dark:border-slate-600 focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 transition-all">
                    </div>

                    <!-- SEO Description -->
                    <div>
                        <label class="block text-sm font-semibold mb-2">Resumen SEO</label>
                        <textarea id="post-desc" rows="3" placeholder="Descripción SEO..." oninput="updateSEO()"
                            class="w-full p-3 rounded-lg border border-gray-200 dark:bg-slate-800 dark:border-slate-600 focus:border-orange-500 focus:ring-2 focus:ring-orange-500/20 transition-all"></textarea>
                    </div>

                    <!-- Date & Author -->
                    <div class="grid grid-cols-2 gap-3">
                        <div>
                            <label class="block text-sm font-semibold mb-2">Fecha</label>
                            <input type="date" id="post-date"
                                class="w-full p-3 rounded-lg border border-gray-200 dark:bg-slate-800 dark:border-slate-600 text-sm">
                        </div>
                        <div>
                            <label class="block text-sm font-semibold mb-2">Autor</label>
                            <select id="post-author"
                                class="w-full p-3 rounded-lg border border-gray-200 dark:bg-slate-800 dark:border-slate-600 text-sm">
                                <option value="">Selecciona...</option>
                            </select>
                        </div>
                    </div>

                    <!-- Tags -->
                    <div>
                        <label class="block text-sm font-semibold mb-2">Tags</label>
                        <input type="text" id="tag-input" placeholder="# Tags (Enter)"
                            class="w-full p-3 rounded-lg border border-gray-200 dark:bg-slate-800 dark:border-slate-600 text-sm">
                        <div id="tags-container" class="flex flex-wrap gap-2 mt-2"></div>
                    </div>

                    <!-- Featured Image -->
                    <div>
                        <label class="block text-sm font-semibold mb-2">Imagen Destacada</label>
                        <input type="text" id="post-hero" placeholder="URL de la imagen"
                            class="w-full p-3 rounded-lg border border-gray-200 dark:bg-slate-800 dark:border-slate-600 text-sm mb-2">
                        <input type="file" id="hero-upload" accept="image/*" class="hidden"
                            onchange="uploadHeroImage(this)">
                        <button type="button" onclick="document.getElementById('hero-upload').click()"
                            class="w-full px-4 py-2 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-lg text-sm font-bold hover:bg-blue-200 dark:hover:bg-blue-900/50 transition-colors flex items-center justify-center gap-2">
                            <ion-icon name="cloud-upload-outline" class="text-lg"></ion-icon>
                            Subir Imagen
                        </button>
                        <div id="hero-preview" class="mt-3 rounded-lg overflow-hidden hidden">
                            <img id="hero-preview-img" class="w-full h-auto" alt="Preview">
                        </div>
                    </div>

                    <!-- Table of Contents -->
                    <div class="border-t border-gray-200 dark:border-slate-700 pt-4">
                        <label class="block text-sm font-semibold mb-3">Índice</label>
                        <div id="toc-list" class="space-y-2 text-sm">
                            <div class="text-gray-400">El índice se generará automáticamente</div>
                        </div>
                    </div>

                    <!-- Reading Time -->
                    <div class="text-sm text-gray-500">
                        <span class="font-semibold">Tiempo estimado:</span>
                        <span id="reading-time">0 min</span>
                    </div>

                    <!-- Actions -->
                    <div class="border-t border-gray-200 dark:border-slate-700 pt-4 space-y-2">
                        <button onclick="openLoadModal()"
                            class="w-full px-4 py-2 bg-gray-100 dark:bg-slate-800 text-gray-700 dark:text-gray-300 rounded-lg text-sm font-bold hover:bg-gray-200 dark:hover:bg-slate-700 transition-colors">
                            Cargar Post
                        </button>
                        <button onclick="clearAll()"
                            class="w-full px-4 py-2 bg-gray-100 dark:bg-slate-800 text-gray-700 dark:text-gray-300 rounded-lg text-sm font-bold hover:bg-gray-200 dark:hover:bg-slate-700 transition-colors">
                            Limpiar Todo
                        </button>
                    </div>

                </div>
            </aside>

            <!-- 3. Editor Area (Center) -->
            <main id="editor-area" class="flex-1 bg-gray-50 dark:bg-slate-800 overflow-y-auto">
                <div class="max-w-4xl mx-auto p-8">

                    <!-- Article Title (Large) -->
                    <h1 id="article-title-display" class="text-4xl font-bold mb-6 text-gray-900 dark:text-white">
                        Título del Artículo
                    </h1>

                    <!-- Hero Image -->
                    <div id="hero-display" class="mb-8 rounded-xl overflow-hidden hidden">
                        <img id="hero-display-img" class="w-full h-auto" alt="Hero">
                    </div>

                    <!-- Tiptap Toolbar -->
                    <div id="tiptap-toolbar"
                        class="flex flex-wrap gap-1 p-3 bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 rounded-lg mb-4">
                        <!-- Text Formatting -->
                        <button onclick="window.tiptapService?.editor?.chain().focus().toggleBold().run()"
                            class="toolbar-btn" title="Negrita (Ctrl+B)">
                            <strong>B</strong>
                        </button>
                        <button onclick="window.tiptapService?.editor?.chain().focus().toggleItalic().run()"
                            class="toolbar-btn" title="Cursiva (Ctrl+I)">
                            <em>I</em>
                        </button>
                        <button onclick="window.tiptapService?.editor?.chain().focus().toggleStrike().run()"
                            class="toolbar-btn" title="Tachado">
                            <s>S</s>
                        </button>

                        <div class="w-px h-6 bg-gray-300 dark:bg-slate-600 mx-1"></div>

                        <!-- Headings -->
                        <button
                            onclick="window.tiptapService?.editor?.chain().focus().toggleHeading({ level: 1 }).run()"
                            class="toolbar-btn" title="Título 1">H1</button>
                        <button
                            onclick="window.tiptapService?.editor?.chain().focus().toggleHeading({ level: 2 }).run()"
                            class="toolbar-btn" title="Título 2">H2</button>
                        <button
                            onclick="window.tiptapService?.editor?.chain().focus().toggleHeading({ level: 3 }).run()"
                            class="toolbar-btn" title="Título 3">H3</button>

                        <div class="w-px h-6 bg-gray-300 dark:bg-slate-600 mx-1"></div>

                        <!-- Lists -->
                        <button onclick="window.tiptapService?.editor?.chain().focus().toggleBulletList().run()"
                            class="toolbar-btn" title="Lista con viñetas">
                            <ion-icon name="list-outline"></ion-icon>
                        </button>
                        <button onclick="window.tiptapService?.editor?.chain().focus().toggleOrderedList().run()"
                            class="toolbar-btn" title="Lista numerada">
                            <ion-icon name="list-outline"></ion-icon>
                            <span class="text-xs">1.</span>
                        </button>

                        <div class="w-px h-6 bg-gray-300 dark:bg-slate-600 mx-1"></div>

                        <!-- Blocks Button -->
                        <button onclick="toggleBlocksMenu()"
                            class="toolbar-btn bg-orange-500 text-white hover:bg-orange-600" title="Insertar Bloque">
                            <ion-icon name="add-circle-outline"></ion-icon>
                            <span class="text-xs">Bloques</span>
                        </button>
                    </div>

                    <!-- Tiptap Editor -->
                    <div id="editor"
                        class="tiptap-editor w-full min-h-[600px] p-6 bg-white dark:bg-slate-900 border border-gray-200 dark:border-slate-700 rounded-lg focus-within:border-orange-500 transition-colors">
                    </div>

                    <!-- Floating Blocks Menu -->
                    <div id="blocks-menu"
                        class="hidden fixed inset-0 bg-black/50 backdrop-blur-sm z-50 flex items-center justify-center"
                        onclick="if(event.target === this) toggleBlocksMenu()">
                        <div class="bg-white dark:bg-slate-900 rounded-2xl shadow-2xl p-6 max-w-2xl w-full mx-4">
                            <div class="flex items-center justify-between mb-6">
                                <h3 class="text-xl font-bold">Insertar Bloque</h3>
                                <button onclick="toggleBlocksMenu()"
                                    class="p-2 hover:bg-gray-100 dark:hover:bg-slate-800 rounded-lg">
                                    <ion-icon name="close-outline" class="text-2xl"></ion-icon>
                                </button>
                            </div>
                            <div class="grid grid-cols-3 gap-4">
                                <button class="block-item" onclick="insertBlock('video')">
                                    <ion-icon name="videocam-outline"></ion-icon>
                                    <span>Video</span>
                                </button>
                                <button class="block-item" onclick="insertBlock('cta')">
                                    <ion-icon name="navigate-outline"></ion-icon>
                                    <span>CTA Destino</span>
                                </button>
                                <button class="block-item" onclick="insertBlock('gallery')">
                                    <ion-icon name="images-outline"></ion-icon>
                                    <span>Galería</span>
                                </button>
                                <button class="block-item" onclick="insertBlock('table')">
                                    <ion-icon name="grid-outline"></ion-icon>
                                    <span>Tabla</span>
                                </button>
                                <button class="block-item" onclick="insertBlock('flights')">
                                    <ion-icon name="airplane-outline"></ion-icon>
                                    <span>Widget Vuelos</span>
                                </button>
                                <button class="block-item" onclick="insertBlock('faq')">
                                    <ion-icon name="help-circle-outline"></ion-icon>
                                    <span>FAQ</span>
                                </button>
                                <button class="block-item" onclick="insertBlock('quote')">
                                    <ion-icon name="chatbox-outline"></ion-icon>
                                    <span>Cita</span>
                                </button>
                                <button class="block-item" onclick="insertBlock('map')">
                                    <ion-icon name="map-outline"></ion-icon>
                                    <span>Mapa</span>
                                </button>
                                <button class="block-item" onclick="insertBlock('proscons')">
                                    <ion-icon name="list-outline"></ion-icon>
                                    <span>Pros & Contras</span>
                                </button>
                            </div>
                        </div>
                    </div>

                </div>
            </main>

            <!-- 4. Preview Panel (Right) -->
            <aside id="preview-panel"
                class="w-[420px] bg-white dark:bg-slate-900 border-l border-gray-200 dark:border-slate-700 overflow-y-auto">
                <div class="p-6 space-y-6">

                    <!-- Preview Header -->
                    <div>
                        <h2 class="text-lg font-bold mb-3">Vista Previa</h2>
                        <div class="flex gap-2">
                            <button class="preview-device-btn active" data-device="desktop">
                                💻 Desktop
                            </button>
                            <button class="preview-device-btn" data-device="tablet">
                                📱 Tablet
                            </button>
                            <button class="preview-device-btn" data-device="mobile">
                                📱 Mobile
                            </button>
                        </div>
                    </div>

                    <!-- Preview Content -->
                    <div id="preview-container"
                        class="border border-gray-200 dark:border-slate-700 rounded-lg overflow-hidden">
                        <div id="live-preview-content"
                            class="p-4 prose prose-sm dark:prose-invert max-w-none bg-white dark:bg-slate-800"
                            style="transform-origin: top left;">
                            <p class="text-gray-400 text-center">La vista previa aparecerá aquí...</p>
                        </div>
                    </div>

                    <!-- SEO Score -->
                    <div class="border border-gray-200 dark:border-slate-700 rounded-lg p-4">
                        <div class="flex items-center justify-between mb-3">
                            <h3 class="font-bold">SEO Score</h3>
                            <div id="seo-score-badge"
                                class="px-3 py-1 bg-orange-100 dark:bg-orange-900/30 text-orange-600 dark:text-orange-400 rounded-full text-sm font-bold">
                                0
                            </div>
                        </div>
                        <div id="seo-alerts" class="space-y-2 text-sm">
                            <!-- Alerts will be inserted here -->
                        </div>
                    </div>

                    <!-- Google Snippet Preview -->
                    <div class="border border-gray-200 dark:border-slate-700 rounded-lg p-4">
                        <h3 class="font-bold mb-3 text-sm">Google Snippet</h3>
                        <div class="space-y-1">
                            <div class="text-xs text-gray-500">www.instantetrips.com</div>
                            <div id="snippet-title" class="text-blue-600 text-sm font-medium">Título del post</div>
                            <div id="snippet-description" class="text-xs text-gray-600">Descripción SEO del artículo...
                            </div>
                        </div>
                    </div>

                </div>
            </aside>

        </div>
        <!-- End V7 Layout -->

    </div>
    <!-- End App Section -->

    <!-- Load Post Modal -->
    <aside id="v7-sidebar">
        <!-- Sidebar Header -->
        <div class="sidebar-header">
            <div class="flex items-center gap-3">
                <div class="bg-gradient-to-br from-orange-500 to-pink-500 p-2 rounded-lg">
                    <img src="public/blog.svg" alt="Logo" class="h-4 invert">
                </div>
                <div id="sidebar-logo-text">
                    <span class="font-bold text-sm">Editor V7</span>
                    <p class="text-[10px] text-gray-500 dark:text-gray-400">Tiptap Powered</p>
                </div>
            </div>
        </div>

        <!-- Sidebar Tabs -->
        <nav class="sidebar-tabs">
            <button class="sidebar-tab-btn active" data-tab="editor">
                <ion-icon name="create-outline"></ion-icon>
                <span>Editor</span>
            </button>
            <button class="sidebar-tab-btn" data-tab="preview">
                <ion-icon name="eye-outline"></ion-icon>
                <span>Preview</span>
            </button>
            <button class="sidebar-tab-btn" data-tab="seo">
                <ion-icon name="analytics-outline"></ion-icon>
                <span>SEO</span>
            </button>
            <button class="sidebar-tab-btn" data-tab="bloques">
                <ion-icon name="grid-outline"></ion-icon>
                <span>Bloques</span>
            </button>
        </nav>

        <!-- Collapse Button -->
        <button class="sidebar-collapse-btn" id="sidebar-collapse-btn">
            <ion-icon name="chevron-back-outline"></ion-icon>
        </button>
    </aside>

    <!-- Main Content -->
    <div id="v7-main-content" class="min-h-screen p-6">
        <div class="max-w-[1800px] mx-auto">

            <!-- Header -->
            <div class="glass-card p-6 rounded-2xl shadow-lg mb-6 flex items-center justify-between">
                <div class="flex items-center gap-4">
                    <div class="bg-gradient-to-br from-orange-500 to-pink-500 p-3 rounded-xl shadow-md">
                        <img src="public/blog.svg" alt="Logo" class="h-5 invert">
                    </div>
                    <div>
                        <span class="font-bold text-sm">Editor V6.4</span>
                        <p class="text-[10px] text-gray-500" id="save-status"></p>
                    </div>
                </div>

                <div class="flex items-center gap-3">
                    <span class="text-sm font-medium hidden sm:block" id="user-greeting"></span>
                    <button onclick="toggleDarkMode()"
                        class="p-3 rounded-xl bg-gray-100 dark:bg-slate-800 hover:bg-gray-200 dark:hover:bg-slate-700 transition-colors">
                        <ion-icon name="moon-outline" class="text-xl dark:hidden"></ion-icon>
                        <ion-icon name="sunny-outline" class="text-xl hidden dark:block"></ion-icon>
                    </button>
                    <button onclick="logout()"
                        class="px-4 py-2 bg-red-100 dark:bg-red-900/30 text-red-600 dark:text-red-400 rounded-xl text-sm font-bold hover:bg-red-200 dark:hover:bg-red-900/50 transition-colors">
                        Salir
                    </button>
                </div>
            </div>

            <!-- Main Grid -->
            <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">

                <!-- TAB: Editor -->
                <div id="editor-tab" class="tab-content lg:col-span-2 space-y-6">

                    <!-- Left Column: Form -->

                    <!-- Post Metadata -->
                    <div
                        class="bg-white dark:bg-slate-900 rounded-2xl shadow-lg border border-gray-200 dark:border-slate-800 p-6 card-hover">
                        <h2 class="text-lg font-bold mb-4 flex items-center gap-2">
                            <ion-icon name="document-text-outline" class="text-xl"></ion-icon>
                            Metadatos del Post
                        </h2>

                        <div class="space-y-4">
                            <input type="text" id="post-title" placeholder="Título del post..." oninput="updateSEO()"
                                class="w-full p-3 rounded-xl accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600">

                            <textarea id="post-desc" rows="2"
                                class="w-full p-3 rounded-xl accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600"
                                placeholder="Descripción SEO..." oninput="updateSEO()"></textarea>

                            <div class="grid grid-cols-2 gap-4">
                                <input type="date" id="post-date"
                                    class="p-3 rounded-xl accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600">
                                <select id="post-author"
                                    class="p-3 rounded-xl accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600">
                                    <option value="">Selecciona autor...</option>
                                </select>
                            </div>

                            <input type="text" id="post-hero" placeholder="Imagen Principal (URL)"
                                class="w-full p-3 rounded-xl accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600">

                            <div class="flex gap-2">
                                <input type="file" id="hero-upload" accept="image/*" class="hidden"
                                    onchange="uploadHeroImage(this)">
                                <button type="button" onclick="document.getElementById('hero-upload').click()"
                                    class="flex-1 px-4 py-2 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-xl text-sm font-bold hover:bg-blue-200 dark:hover:bg-blue-900/50 transition-colors flex items-center justify-center gap-2">
                                    <ion-icon name="cloud-upload-outline" class="text-lg"></ion-icon>
                                    Subir Imagen
                                </button>
                            </div>

                            <!-- Tags -->
                            <div>
                                <input type="text" id="tag-input" placeholder="# Tags (Enter)"
                                    class="w-full p-3 rounded-xl accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600">
                                <div id="tags-container" class="flex flex-wrap gap-2 mt-2"></div>
                            </div>

                            <!-- Widgets de Viaje -->
                            <div class="border-t border-gray-200 dark:border-slate-700 pt-4">
                                <h3 class="text-sm font-bold mb-3 flex items-center gap-2">
                                    <ion-icon name="airplane-outline" class="text-lg"></ion-icon>
                                    WIDGETS DE VIAJE ✈️ COPIAR CÓDIGO
                                </h3>
                                <div class="space-y-2">
                                    <input type="text" id="flight-dest" placeholder="Destino Vuelos (Ej: Madrid)"
                                        class="w-full p-2 rounded-lg text-sm accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600">
                                    <input type="text" id="tour-city" placeholder="Ciudad Tours (Ej: Madrid)"
                                        class="w-full p-2 rounded-lg text-sm accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600">
                                </div>
                            </div>

                            <!-- Video Button -->
                            <div class="pt-2">
                                <button onclick="insertVideoLink()"
                                    class="flex items-center gap-2 px-3 py-2 bg-red-100 dark:bg-red-900/30 text-red-600 dark:text-red-400 rounded-lg text-sm font-bold hover:bg-red-200 dark:hover:bg-red-900/50 transition-colors w-full justify-center">
                                    <ion-icon name="logo-youtube" class="text-lg"></ion-icon> Insertar Video
                                    (YouTube /
                                    TikTok)
                                </button>
                            </div>
                        </div>
                    </div>

                    <div
                        class="bg-white dark:bg-slate-900 rounded-2xl shadow-lg border border-gray-200 dark:border-slate-800 overflow-hidden min-h-[500px] card-hover">

                        <!-- Tiptap Toolbar -->
                        <div id="tiptap-toolbar"
                            class="flex flex-wrap gap-1 p-3 bg-gray-50 dark:bg-slate-800 border-b border-gray-200 dark:border-slate-700">
                            <!-- Text Formatting -->
                            <button onclick="window.tiptapService?.editor?.chain().focus().toggleBold().run()"
                                class="toolbar-btn" title="Negrita (Ctrl+B)">
                                <ion-icon name="text-outline" class="font-bold"></ion-icon>
                                <strong>B</strong>
                            </button>
                            <button onclick="window.tiptapService?.editor?.chain().focus().toggleItalic().run()"
                                class="toolbar-btn" title="Cursiva (Ctrl+I)">
                                <em>I</em>
                            </button>
                            <button onclick="window.tiptapService?.editor?.chain().focus().toggleStrike().run()"
                                class="toolbar-btn" title="Tachado">
                                <s>S</s>
                            </button>

                            <div class="w-px h-6 bg-gray-300 dark:bg-slate-600 mx-1"></div>

                            <!-- Headings -->
                            <button
                                onclick="window.tiptapService?.editor?.chain().focus().toggleHeading({ level: 1 }).run()"
                                class="toolbar-btn" title="Título 1">
                                H1
                            </button>
                            <button
                                onclick="window.tiptapService?.editor?.chain().focus().toggleHeading({ level: 2 }).run()"
                                class="toolbar-btn" title="Título 2">
                                H2
                            </button>
                            <button
                                onclick="window.tiptapService?.editor?.chain().focus().toggleHeading({ level: 3 }).run()"
                                class="toolbar-btn" title="Título 3">
                                H3
                            </button>

                            <div class="w-px h-6 bg-gray-300 dark:bg-slate-600 mx-1"></div>

                            <!-- Lists -->
                            <button onclick="window.tiptapService?.editor?.chain().focus().toggleBulletList().run()"
                                class="toolbar-btn" title="Lista con viñetas">
                                <ion-icon name="list-outline"></ion-icon>
                            </button>
                            <button onclick="window.tiptapService?.editor?.chain().focus().toggleOrderedList().run()"
                                class="toolbar-btn" title="Lista numerada">
                                <ion-icon name="list-outline"></ion-icon>
                                <span class="text-xs">1.</span>
                            </button>

                            <div class="w-px h-6 bg-gray-300 dark:bg-slate-600 mx-1"></div>

                            <!-- Blockquote & Code -->
                            <button onclick="window.tiptapService?.editor?.chain().focus().toggleBlockquote().run()"
                                class="toolbar-btn" title="Cita">
                                <ion-icon name="chatbox-outline"></ion-icon>
                            </button>
                            <button onclick="window.tiptapService?.editor?.chain().focus().toggleCodeBlock().run()"
                                class="toolbar-btn" title="Bloque de código">
                                <ion-icon name="code-slash-outline"></ion-icon>
                            </button>

                            <div class="w-px h-6 bg-gray-300 dark:bg-slate-600 mx-1"></div>

                            <!-- Undo/Redo -->
                            <button onclick="window.tiptapService?.editor?.chain().focus().undo().run()"
                                class="toolbar-btn" title="Deshacer (Ctrl+Z)">
                                <ion-icon name="arrow-undo-outline"></ion-icon>
                            </button>
                            <button onclick="window.tiptapService?.editor?.chain().focus().redo().run()"
                                class="toolbar-btn" title="Rehacer (Ctrl+Shift+Z)">
                                <ion-icon name="arrow-redo-outline"></ion-icon>
                            </button>
                        </div>

                        <!-- Tiptap Editor -->
                        <div id="editor"
                            class="tiptap-editor w-full min-h-[400px] p-4 bg-white dark:bg-slate-800 focus-within:border-orange-500 dark:focus-within:border-orange-500 transition-colors">
                        </div>
                    </div>

                    <div class="mt-8 mb-8">
                        <button onclick="publishCurrentPost()"
                            class="w-full py-4 bg-gradient-to-r from-green-500 to-emerald-500 text-white font-bold rounded-xl shadow-lg hover:shadow-xl transition-all hover:scale-105 flex items-center justify-center gap-2">
                            <ion-icon name="cloud-upload-outline" class="text-xl"></ion-icon>
                            Publicar Post
                        </button>
                    </div>
                </div>
                <!-- End Editor Tab -->

                <!-- TAB: Preview -->
                <div id="preview-tab" class="tab-content hidden lg:col-span-2 space-y-6">
                    <div
                        class="bg-white dark:bg-slate-900 rounded-2xl shadow-lg border border-gray-200 dark:border-slate-800 overflow-hidden card-hover">
                        <div
                            class="p-4 border-b border-gray-200 dark:border-slate-800 flex items-center justify-between">
                            <h2 class="text-lg font-bold flex items-center gap-2">
                                <ion-icon name="eye-outline" class="text-xl"></ion-icon>
                                Vista Previa
                            </h2>
                            <div class="flex gap-2">
                                <button
                                    class="text-xs px-3 py-1 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-lg font-bold">
                                    💻 Desktop
                                </button>
                                <button
                                    class="text-xs px-3 py-1 bg-gray-100 dark:bg-gray-800 text-gray-600 dark:text-gray-400 rounded-lg font-bold">
                                    📱 Mobile
                                </button>
                            </div>
                        </div>
                        <div id="live-preview-content"
                            class="p-6 prose prose-sm dark:prose-invert max-w-none overflow-auto max-h-[80vh]">
                            <p class="text-gray-400 text-center">La vista previa aparecerá aquí...</p>
                        </div>
                    </div>
                </div>
                <!-- End Preview Tab -->

                <!-- TAB: SEO -->
                <div id="seo-tab" class="tab-content hidden lg:col-span-2 space-y-6">
                    <div
                        class="bg-white dark:bg-slate-900 rounded-2xl shadow-lg border border-gray-200 dark:border-slate-800 p-6 card-hover">
                        <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
                            <ion-icon name="analytics-outline" class="text-3xl"></ion-icon>
                            SEO Dashboard
                        </h2>

                        <div id="seo-score" class="text-center mb-8">
                            <div class="text-6xl font-black mb-2">0</div>
                            <div class="text-sm text-gray-500">Completa los campos</div>
                        </div>

                        <div class="space-y-4">
                            <div class="p-4 bg-gray-50 dark:bg-slate-800 rounded-xl">
                                <h3 class="font-bold mb-2">📝 Análisis de Contenido</h3>
                                <p class="text-sm text-gray-600 dark:text-gray-400">Escribe contenido para ver el
                                    análisis SEO</p>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- End SEO Tab -->

                <!-- TAB: Bloques -->
                <div id="bloques-tab" class="tab-content hidden lg:col-span-2 space-y-6">
                    <div
                        class="bg-white dark:bg-slate-900 rounded-2xl shadow-lg border border-gray-200 dark:border-slate-800 p-6 card-hover">
                        <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
                            <ion-icon name="grid-outline" class="text-3xl"></ion-icon>
                            Biblioteca de Bloques
                        </h2>

                        <div class="text-center text-gray-400 py-12">
                            <ion-icon name="construct-outline" class="text-6xl mb-4"></ion-icon>
                            <p>Panel de bloques en construcción...</p>
                            <p class="text-sm mt-2">Próximamente: Drag & drop de componentes</p>
                        </div>
                    </div>
                </div>
                <!-- End Bloques Tab -->

                <!-- Right Column: Preview & Actions -->
                <div class="space-y-6">

                    <!-- Actions -->
                    <div
                        class="bg-white dark:bg-slate-900 rounded-2xl shadow-lg border border-gray-200 dark:border-slate-800 p-6 card-hover">
                        <h2 class="text-lg font-bold mb-4 flex items-center gap-2">
                            <ion-icon name="settings-outline" class="text-xl"></ion-icon>
                            Acciones
                        </h2>
                        <div class="space-y-3">
                            <button onclick="openLoadModal()"
                                class="w-full px-4 py-3 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-xl text-sm font-bold hover:bg-blue-200 dark:hover:bg-blue-900/50 transition-colors flex items-center justify-center gap-2">
                                <ion-icon name="folder-open-outline" class="text-lg"></ion-icon>
                                Cargar Post
                            </button>
                            <button onclick="clearAll()"
                                class="w-full px-4 py-3 bg-gray-100 dark:bg-slate-800 text-gray-600 dark:text-gray-400 rounded-xl text-sm font-bold hover:bg-gray-200 dark:hover:bg-slate-700 transition-colors flex items-center justify-center gap-2">
                                <ion-icon name="trash-outline" class="text-lg"></ion-icon>
                                Limpiar Todo
                            </button>
                        </div>
                    </div>

                    <!-- SEO Score -->
                    <div
                        class="bg-white dark:bg-slate-900 rounded-2xl shadow-lg border border-gray-200 dark:border-slate-800 p-6 card-hover">
                        <h2 class="text-lg font-bold mb-4 flex items-center gap-2">
                            <ion-icon name="analytics-outline" class="text-xl"></ion-icon>
                            SEO Score
                        </h2>
                        <div id="seo-score" class="text-center">
                            <div class="text-5xl font-black mb-2">0</div>
                            <div class="text-sm text-gray-500">Completa los campos</div>
                        </div>
                    </div>

                    <!-- Live Preview -->
                    <div
                        class="bg-white dark:bg-slate-900 rounded-2xl shadow-lg border border-gray-200 dark:border-slate-800 overflow-hidden card-hover">
                        <div
                            class="p-4 border-b border-gray-200 dark:border-slate-800 flex items-center justify-between">
                            <h2 class="text-sm font-bold flex items-center gap-2">
                                <ion-icon name="eye-outline" class="text-lg"></ion-icon>
                                VISTA PREVIA EN VIVO
                            </h2>
                            <button onclick="openMobilePreview()"
                                class="text-xs px-3 py-1 bg-purple-100 dark:bg-purple-900/30 text-purple-600 dark:text-purple-400 rounded-lg font-bold hover:bg-purple-200 dark:hover:bg-purple-900/50 transition-colors">
                                📱 Móvil
                            </button>
                        </div>
                        <div id="live-preview-content"
                            class="p-6 prose prose-sm dark:prose-invert max-w-none overflow-auto max-h-[600px]">
                            <p class="text-gray-400 text-center">La vista previa aparecerá aquí...</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Load Post Modal -->
    <div id="load-post-modal" class="modal">
        <div class="glass-card p-8 rounded-3xl shadow-2xl w-full max-w-2xl max-h-[80vh] overflow-auto">
            <div class="flex items-center justify-between mb-6">
                <h2 class="text-2xl font-bold">Cargar Post</h2>
                <button onclick="closeLoadModal()"
                    class="p-2 rounded-xl hover:bg-gray-100 dark:hover:bg-slate-800 transition-colors">
                    <ion-icon name="close-outline" class="text-2xl"></ion-icon>
                </button>
            </div>

            <input type="text" id="search-posts" placeholder="Buscar posts..." oninput="filterPosts()"
                class="w-full p-3 rounded-xl accent-input border border-gray-200 dark:bg-slate-800 dark:border-slate-600 mb-4">

            <div id="posts-list" class="space-y-2"></div>
        </div>
    </div>

    <!-- Mobile Preview Modal -->
    <div id="preview-modal-mobile" class="modal">
        <div class="bg-white dark:bg-slate-900 rounded-3xl shadow-2xl w-full max-w-md max-h-[90vh] overflow-auto">
            <div
                class="p-4 border-b border-gray-200 dark:border-slate-800 flex items-center justify-between sticky top-0 bg-white dark:bg-slate-900">
                <h2 class="text-lg font-bold">Vista Previa Móvil</h2>
                <button onclick="closeMobilePreview()"
                    class="p-2 rounded-xl hover:bg-gray-100 dark:hover:bg-slate-800 transition-colors">
                    <ion-icon name="close-outline" class="text-2xl"></ion-icon>
                </button>
            </div>
            <div id="mobile-preview-content" class="p-6 prose prose-sm dark:prose-invert max-w-none"></div>
        </div>
    </div>

    <div id="toast-container" class="fixed bottom-6 left-6 z-[80] flex flex-col gap-2"></div>

    <!-- Modular JavaScript -->
    <script type="module" src="js/main.js"></script>

</body>

</html>